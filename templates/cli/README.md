# CLI Tool

## Run

```bash
make run ARGS="--help"
make run ARGS="hello --name Francesco"
```

## Develop

```bash
make dev     # auto-restart on file changes
make test    # run tests
make shell   # drop into container shell
```
