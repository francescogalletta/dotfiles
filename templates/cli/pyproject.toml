[project]
name = "cli"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "typer>=0.13",
    "rich>=13.0",
    "python-dotenv>=1.0",
    "watchdog>=5.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]
