# [T103] Slim /ship skill to minimum viable

**Phase:** Phase 5: Agent config sanity pass
**Status:** `done`
**Owner:** @claude
**BlockedBy:** —
**Validation:** user reviews resulting skill before merge

## Goal

Cut `/ship` from 60 lines to ~15. Most of the skill reimplements built-in commit/push behaviour. The novel parts are: spartan one-line message (no type prefix, no scope, no period, ≤72 chars) and explicit confirmation before commit. Strip everything else.

Also fix the duplicate `## Step 3` header (currently both "Propose a commit message" and "Commit and push" are labelled Step 3).

## Acceptance Criteria

- [ ] `config/claude/skills/ship/SKILL.md` is ≤ 20 lines (excluding frontmatter)
- [ ] No duplicate step headers
- [ ] Frontmatter `allowed-tools` retained
- [ ] Behaviour preserved: read diff → propose spartan one-line → confirm → commit + push
- [ ] User reviews and approves

## Context

The grilling found `/ship` duplicates what Claude does natively on "commit and push", with a duplicate header bug. Slimming keeps the muscle memory (`/ship`) while removing the ceremony.

## Decisions

- 2026-05-21: Rewrote `ship/SKILL.md` to 18 body lines (≤20 target). Collapsed the 3 numbered steps into a single ordered list. Dropped the "Check scope / split commits?" prompt — Claude already raises it in `/ship` when changes look unrelated; baking it in adds ceremony for the common case. Kept the spartan one-line rules verbatim. Replaced the duplicate-`Step 3` headers and the long "if push fails" block with a single sentence covering the `--set-upstream` retry.

## Notes

<!-- Append dated notes when context is clarified. Never edit prior entries — only append. -->

## References

- `config/claude/skills/ship/SKILL.md`
