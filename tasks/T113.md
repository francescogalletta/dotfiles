# [T113] Switch preferred notif channel to terminal_bell for Zed alerts

**Phase:** Phase 5: Agent config sanity pass
**Status:** `done`
**Owner:** @claude
**BlockedBy:** —
**Validation:** user confirms agent alerts now surface through Zed

## Goal

Flip `preferredNotifChannel` in `config/claude/settings.json` from `ghostty` to `terminal_bell` so agent notifications ring the terminal bell that Zed's integrated terminal forwards as an OS notification.

## Acceptance Criteria

- [x] `config/claude/settings.json` sets `preferredNotifChannel: "terminal_bell"`
- [x] Change shipped to `main`

## Context

Agent work increasingly happens in Zed's integrated terminal rather than standalone Ghostty. The `ghostty` channel only fires when Claude Code runs inside Ghostty proper; switching to `terminal_bell` makes notifications portable to any terminal emulator that surfaces the bell, including Zed.

## Decisions

- 2026-05-22: One-line settings flip. No code change needed.

## Notes

<!-- Append dated notes when context is clarified. Never edit prior entries — only append. -->

## References

- `config/claude/settings.json` (`preferredNotifChannel`)
