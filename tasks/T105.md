# [T105] Slim /project-new: drop Blocks, drop empty .claude dirs, keep per-task files

**Phase:** Phase 5: Agent config sanity pass
**Status:** `pending`
**Owner:** —
**BlockedBy:** —
**Validation:** user reviews resulting skill before merge

## Goal

Tighten `/project-new` based on the grilling discussion. Keep the per-task `tasks/T###.md` pattern (it enables progressive disclosure for sub-agent routing) but trim the frontmatter and remove ceremony that adds no value for solo prototypes.

Changes:

1. Drop `Blocks:` from the per-task template (BlockedBy is sufficient; Blocks is redundant)
2. Stop pre-creating empty `.claude/agents/`, `.claude/skills/`, `.claude/rules/` directories
3. Remove the "trigger on any fresh project start" language from the description (model invocation is disabled via `disable-model-invocation: true`, so the description is dead instructions in that mode)
4. Tighten the per-task template body to the minimum needed for sub-agent routing: Goal, Acceptance Criteria, Context, Decisions, Notes, References

## Acceptance Criteria

- [ ] Per-task template no longer mentions `Blocks:`
- [ ] Scaffolding step no longer pre-creates empty `.claude/` subdirectories
- [ ] Description in frontmatter reflects that the skill is user-invoked
- [ ] Skill still ≤ 250 lines (current: 258)
- [ ] User reviews and approves

## Context

The per-task file pattern was challenged in the grilling but justified by the sub-agent routing workflow: scannable TASKS.md → drill into tasks/T###.md for full context. Keeping that. Cutting around it.

## Decisions

<!-- Append dated notes when significant choices are made during this task. Never edit prior entries — only append. -->

## Notes

<!-- Append dated notes when context is clarified. Never edit prior entries — only append. -->

## References

- `config/claude/skills/project-new/SKILL.md`
