# [T106] Slim /learn skill (remove Monzo-specific block, drop memory step)

**Phase:** Phase 5: Agent config sanity pass
**Status:** `pending`
**Owner:** —
**BlockedBy:** —
**Validation:** user reviews resulting skill before merge

## Goal

Cut `/learn` from 85 lines to ~40. Specific cuts:

1. Drop the "Comms check" block referencing `programmes/*/comms/` and `knowledge/comms-voice.md` — that's Monzo work-specific content that doesn't belong in personal dotfiles
2. Drop "Step 5 — Save memories" — the auto-memory system handles this automatically; the skill instruction is redundant
3. Tighten the analysis prompts; keep the "Friction / Patterns / Surprises / What worked well" framing since that's the core value

## Acceptance Criteria

- [ ] No references to `programmes/`, `comms-voice.md`, or other Monzo-specific paths
- [ ] No explicit "save memories" step (auto-memory handles it)
- [ ] Skill ≤ 50 lines
- [ ] Core flow preserved: gather evidence → analyse → propose improvements → apply approved
- [ ] User reviews and approves

## Context

`/learn` and `/explain` overlap in shape (both read git log and produce structured output). They have different intents (`/learn` is retrospective and improvement-focused; `/explain` is forward and educational) so both stay. But `/learn` carries leftover Monzo-specific instructions that don't belong in a personal dotfiles repo.

## Decisions

<!-- Append dated notes when significant choices are made during this task. Never edit prior entries — only append. -->

## Notes

<!-- Append dated notes when context is clarified. Never edit prior entries — only append. -->

## References

- `config/claude/skills/learn/SKILL.md`
